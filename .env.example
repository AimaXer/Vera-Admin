# Local vanity: Admin over HTTPS; API process stays HTTP.
# Same-origin proxy avoids mixed content:
VITE_API_URL=https://vera-admin.pl/__vera_api
# Plain localhost (no Caddy):
# VITE_API_URL=http://127.0.0.1:3000
